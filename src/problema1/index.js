const solution = (arr) => {
    // Erase the lines below and write your own solution
    const answer = {
        media: 3.14,
        moda: 1,
        mediana: 3,
    };
    return answer;
};

module.exports = { solution };
